[package]
name = "fugu-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
assert_cmd = "2"
fugu-core = { path = "../crates/fugu-core" }
clap = { version = "4.6.5", features = ["derive"] }

[dev-dependencies]
rstest = { workspace = true }
tempfile = { workspace = true }